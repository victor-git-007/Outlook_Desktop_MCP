--- a/flagged-only-python-filter.patch+++ b/flagged-only-python-filter.patch@@ -313,6 +313,7 @@     folder: str = "inbox",
     count: int = 10,
     unread_only: bool = False,
+    flagged_only: bool = False,
     start_date: str = "",
     end_date: str = "",
     account: str = "",
@@ -333,6 +334,8 @@             list_folders output.
         count: Maximum number of emails to return. Default 10, max recommended 50.
         unread_only: If true, only return unread emails. Default false.
+        flagged_only: If true, only return emails marked with a follow-up
+            flag (Outlook "IsMarkedAsTask"). Default false.
         start_date: Optional. Only return emails received on or after this date.
             ISO 8601 format (e.g. "2026-03-10" or "2026-03-10 09:00").
         end_date: Optional. Only return emails received on or before this date.
@@ -343,7 +346,7 @@     Returns:
         JSON array of email summary objects.
     """
-    def _list(outlook, namespace, folder, count, unread_only, start_date, end_date, account):
+    def _list(outlook, namespace, folder, count, unread_only, flagged_only, start_date, end_date, account):
         count = min(max(1, count), 200)
         store = _require_store(namespace, account)
         target = _resolve_folder(namespace, folder, store)
@@ -353,7 +356,9 @@         items = target.Items
         items.Sort("[ReceivedTime]", True)
 
-        # Build restriction filters
+        # Build restriction filters (flagged_only is handled post-filter,
+        # because [IsMarkedAsTask] = True is an invalid Restrict condition in
+        # some Outlook installs and throws "Condition is not valid".)
         restrictions = []
         if unread_only:
             restrictions.append("[UnRead] = True")
@@ -374,13 +379,16 @@         limit = min(count, items.Count)
         for i in range(limit):
             try:
-                results.append(format_email_summary(items.Item(i + 1)))
+                item = items.Item(i + 1)
+                if flagged_only and not getattr(item, "IsMarkedAsTask", False):
+                    continue
+                results.append(format_email_summary(item))
             except Exception:
                 continue
         return json.dumps(results, indent=2, default=str)
 
     try:
-        return await bridge.call(_list, folder, count, unread_only, start_date, end_date, account)
+        return await bridge.call(_list, folder, count, unread_only, flagged_only, start_date, end_date, account)
     except Exception as e:
         return f"Error listing emails: {format_com_error(e)}"
 
